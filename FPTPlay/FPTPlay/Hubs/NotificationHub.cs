using Microsoft.AspNetCore.SignalR;
using System.Threading.Tasks;

namespace FPTPlay.Hubs
{
    public class NotificationHub : Hub
    {
        // Hub trống, chỉ dùng để server push tới client
    }
}
